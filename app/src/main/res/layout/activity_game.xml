<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#111111"
    tools:context=".ui.game.GameActivity">

    <!-- TURN INDICATOR -->
    <TextView
        android:id="@+id/tvTurnIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:fontFamily="@font/press_start_2p"
        android:textColor="#F5F5F5"
        android:textSize="20sp"
        android:letterSpacing="0.05"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Turn: X" />

    <!-- SCORE BOARD -->
    <LinearLayout
        android:id="@+id/scoreLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="20dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        app:layout_constraintTop_toBottomOf="@+id/tvTurnIndicator"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvScoreX"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/press_start_2p"
            android:gravity="center"
            android:textColor="#FF5252"
            android:textSize="14sp"
            tools:text="Player X: 0" />

        <TextView
            android:id="@+id/tvScoreO"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/press_start_2p"
            android:gravity="center"
            android:textColor="#40C4FF"
            android:textSize="14sp"
            tools:text="Player O: 0" />
    </LinearLayout>

    <!-- GAME GRID -->
    <GridLayout
        android:id="@+id/gameBoard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="3"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@+id/scoreLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Buttons -->
        <Button android:id="@+id/btnCell00" style="@style/GameCell" />
        <Button android:id="@+id/btnCell01" style="@style/GameCell" />
        <Button android:id="@+id/btnCell02" style="@style/GameCell" />

        <Button android:id="@+id/btnCell10" style="@style/GameCell" />
        <Button android:id="@+id/btnCell11" style="@style/GameCell" />
        <Button android:id="@+id/btnCell12" style="@style/GameCell" />

        <Button android:id="@+id/btnCell20" style="@style/GameCell" />
        <Button android:id="@+id/btnCell21" style="@style/GameCell" />
        <Button android:id="@+id/btnCell22" style="@style/GameCell" />

    </GridLayout>

    <!-- RESET BUTTON -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnResetGame"
        style="@style/PixelButton"
        android:text="RESET GAME"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="20dp"
        app:layout_constraintBottom_toTopOf="@+id/btnExitGame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- EXIT BUTTON -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnExitGame"
        style="@style/PixelButton"
        android:text="EXIT GAME"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
